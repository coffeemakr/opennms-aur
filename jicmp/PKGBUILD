# Maintainer: Your Name <youremail@domain.com>
pkgname="jicmp"
pkgver="1.4.4"
pkgrel=1
pkgdesc="something"
arch=('i686' 'x86_64')
url=""
license=('GPL')
groups=()
depends=('jdk8-openjdk')
makedepends=('autoconf')
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=($pkgname-$pkgver::git+https://github.com/OpenNMS/jicmp.git#tag=jicmp-1.4.4-1)
noextract=()
md5sums=('SKIP')

prepare() {
	cd "$pkgname-$pkgver"
	git submodule update --init --recursive
}

build() {
	cd "$pkgname-$pkgver"
	autoreconf -fvi
	./configure --prefix=/usr --with-java=/usr/lib/jvm/java-8-openjdk
	make
}

package() {
	cd "$pkgname-$pkgver"
	make DESTDIR="$pkgdir/" install
}
